# Math OCR configuration (do not commit real secrets)
# Replace these with your real endpoint and API key before running `npm start` or `npm run build`.
MATH_OCR_ENDPOINT=http://localhost:3001/parse
MATH_OCR_API_KEY=dev-key

# Pix2Text adapter configuration (used by the Node adapter)
P2T_ENDPOINT=http://127.0.0.1:8503/pix2text
P2T_RESIZED_SHAPE=768
STEMVEX_OCR_ADAPTER_API_KEY=dev-key
